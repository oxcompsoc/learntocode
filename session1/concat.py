name = "Thomas"
age = 19

# If you uncomment the following line of code then it won't run
# print(name + " is " + age + " years old")
# You need to do this instead to concatenate (join) strings and numbers:
print(name + " is " + str(age) + " years old")
